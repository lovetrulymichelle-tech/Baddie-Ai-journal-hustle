# ============================================================================
# Environment Configuration for Baddie AI Journal Hustle
# ============================================================================
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env:  cp .env.example .env
# 2. Update the values below with your actual credentials
# 3. NEVER commit .env to git - it's already in .gitignore
#
# SECURITY WARNING:
# - Do NOT put API keys as comments (e.g., # OPENAI_API_KEY=sk-xxx)
# - Do NOT commit .env to version control
# - Do revoke and regenerate any exposed API keys immediately
# - Use the format: VARIABLE_NAME=value (no spaces around =)
#
# ============================================================================

# Flask Configuration
# Set to false in production for security
FLASK_DEBUG=false

# Application Security
# Generate a secure random string for production
# Command: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-very-secure-random-key-change-in-production

# Database Configuration
# For production with PostgreSQL (Railway, Heroku, etc.)
# Use postgresql:// prefix for SQLAlchemy compatibility
# SQLALCHEMY_DATABASE_URI=postgresql://username:password@host:port/database
#
# For local development with SQLite (default if not set)
# SQLALCHEMY_DATABASE_URI=sqlite:///journal.db

# Admin Access (Optional)
# Password for admin access features
# ADMIN_PASSWORD=your-secure-admin-password

# ============================================================================
# AI Integration (Optional)
# ============================================================================
# OpenAI API key for AI-powered insights and analysis
# 
# HOW TO GET AN API KEY:
# 1. Go to: https://platform.openai.com/api-keys
# 2. Sign up or log in to your OpenAI account
# 3. Create a new API key
# 4. Copy the key and paste it below (it starts with sk-)
#
# CORRECT FORMAT (uncomment and replace with your key):
# OPENAI_API_KEY=sk-your-actual-api-key-here
#
# WRONG FORMATS (DO NOT USE):
# ❌ #OPENAI_API_KEY=sk-... (comment won't work)
# ❌ OPENAI_API_KEYsk-...     (missing = sign)
# ❌ OPENAI_API_KEY = sk-...  (spaces around = won't work in some parsers)
#
# The key should be:
# - Uncommented (no # at the start of the line)
# - Have no spaces around the = sign
# - Be on a single line
#
# Example of correct format:
# OPENAI_API_KEY=sk-proj-abc123xyz456...
# ============================================================================

# Server Configuration (Optional)
# HOST=0.0.0.0
# PORT=5000

# ============================================================================
# Production Deployment Checklist:
# ============================================================================
# [ ] Set FLASK_DEBUG=false
# [ ] Generate and set a strong SECRET_KEY
# [ ] Configure SQLALCHEMY_DATABASE_URI for your database
# [ ] Set OPENAI_API_KEY if using AI features
# [ ] Verify .env is NOT committed to git
# [ ] For Railway: Use PostgreSQL connection string from database service
# [ ] For local dev: Can use SQLite (default) or omit SQLALCHEMY_DATABASE_URI
# ============================================================================